<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/kp_iv_album_cover"
        android:layout_width="@dimen/kp_popup_cover_size"
        android:layout_height="@dimen/kp_popup_cover_size"
        android:contentDescription="@string/kp_album_cover"/>

    <TextView
        android:id="@+id/kp_tv_album_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="15dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/kp_popup_text"
        android:textSize="@dimen/kp_popup_title_size"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@+id/kp_iv_album_cover"/>
</RelativeLayout>